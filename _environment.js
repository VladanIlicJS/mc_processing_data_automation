const envDev = {
  moveToRejected: false,
  moveToProcessed:false,
  createDraft: true,
};

const envProd = {
  moveToRejected: true,
  moveToProcessed:true,
  createDraft: false,
};

const environment = envDev;